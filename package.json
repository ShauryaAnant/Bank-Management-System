{
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "scripts": {
    "install-all": "npm install && cd cpp-addon && npm install && cd ../bank-frontend && npm install && cd .."
  }
}
